# CivicUtility Agent Notes

Do not implement utility billing, payment processing, rate-engine behavior, account writes, shutoff/reconnect decisions, live LLM calls, live billing connectors, or Civic311 write-back in v0.1.0.

Use `civiccore==0.2.0` and do not import from CivicCore placeholder packages.
